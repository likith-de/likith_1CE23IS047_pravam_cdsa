#include<stdio.h>

int main()
{
    int n=0,a[100],esum=0,osum=0;
    for(int i=0;i<n;scanf("%d",a[i=1]));

    for(int i=0;i<0;i++)
    {
        if(a[i]%==0)
            esum+=a[i];
        else
            osum+=a[i];int n=0,a[100],sum=0;
    }
   

    printf("%d" "%d",esum,osum)
    return 0;
}